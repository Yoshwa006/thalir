package com.example.thalir.repo;

import com.example.thalir.model.PurchasedModel;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PurchasedModelRepo extends JpaRepository<PurchasedModel, Long>{
}
